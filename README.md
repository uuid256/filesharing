# File Storage System

ระบบจัดการไฟล์และโฟลเดอร์แบบครบวงจร พัฒนาด้วย NestJS และ Next.js

## 🚀 คุณสมบัติหลัก

- ✅ อัปโหลดไฟล์ (เดี่ยว, หลายไฟล์, โฟลเดอร์)
- ✅ จัดการโฟลเดอร์แบบลำดับชั้น
- ✅ ดาวน์โหลดไฟล์และโฟลเดอร์ (ZIP)
- ✅ แชร์ไฟล์ผ่านลิงก์สาธารณะ
- ✅ Soft Delete และกู้คืนไฟล์
- ✅ รองรับไฟล์ขนาดใหญ่ถึง 100GB
- ✅ Multi-tenant support
- ✅ Swagger API Documentation

## 📦 เทคโนโลยี

### Backend
- **NestJS** 11.0.1
- **MongoDB** 8.18.1 with Mongoose
- **Multer** 2.0.2 (File Upload)
- **Swagger/OpenAPI** (API Documentation)
- **TypeScript** 5.7.3

### Frontend
- **Next.js** 15.5.4 (App Router)
- **React** 19.1.0
- **Tailwind CSS** 4.x
- **TypeScript** 5.x

## 🛠️ การติดตั้ง

### วิธีที่ 1: Docker (แนะนำ) 🐳

**ข้อกำหนดเบื้องต้น**: Docker และ Docker Compose

```bash
# 1. Clone repository
git clone <repository-url>
cd DBD

# 2. คัดลอกไฟล์ environment
cp .env.example .env

# 3. เริ่มทุกอย่างด้วยคำสั่งเดียว!
docker compose up

# หรือใช้ Make (ถ้ามี)
make dev
```

**เข้าถึงแอปพลิเคชัน**:
- Frontend: http://localhost:3000
- Backend API: http://localhost:3001/api
- Mongo Express (DB Admin): http://localhost:8081

**คำสั่งที่ใช้บ่อย**:
```bash
make dev          # เริ่มแบบมี logs
make up           # เริ่มแบบ background
make down         # หยุดทุก services
make logs         # ดู logs
make clean        # ลบทุกอย่าง (รวมข้อมูล)
```

📖 **เอกสารเพิ่มเติม**: ดูที่ [DOCKER.md](./DOCKER.md)

---

### วิธีที่ 2: Manual Setup (สำหรับ Development)

### ข้อกำหนดเบื้องต้น
- Node.js 18+
- Docker (สำหรับ MongoDB)
- npm หรือ yarn

### 1. ติดตั้ง Backend

```bash
cd backend

# ติดตั้ง dependencies
npm install

# สร้างไฟล์ .env (คัดลอกจาก .env.example)
cp .env.example .env

# เริ่ม MongoDB ด้วย Docker
docker-compose up -d

# เริ่มเซิร์ฟเวอร์
npm run start:dev
```

Backend จะทำงานที่: http://localhost:3001
Swagger Docs: http://localhost:3001/api

### 2. ติดตั้ง Frontend

```bash
cd frontend

# ติดตั้ง dependencies
npm install

# เริ่มเซิร์ฟเวอร์
npm run dev
```

Frontend จะทำงานที่: http://localhost:3000

## 📁 โครงสร้างโปรเจค

```
.
├── backend/                    # NestJS Backend
│   ├── src/
│   │   ├── files/             # Files Module
│   │   ├── folders/           # Folders Module
│   │   ├── share/             # Share Module
│   │   ├── config/            # Configuration
│   │   └── main.ts            # Entry Point
│   ├── uploads/               # File Storage
│   └── docker-compose.yml     # MongoDB Setup
│
└── frontend/                   # Next.js Frontend
    ├── app/
    │   ├── page.tsx           # Home Page
    │   ├── upload/            # Upload Page
    │   ├── files/             # Files Management
    │   ├── trash/             # Trash Page
    │   └── activities/        # Activities Page
    └── package.json

```

## 🔌 API Endpoints

### Files
- `POST /files/upload` - อัปโหลดไฟล์เดี่ยว
- `POST /files/upload/multiple` - อัปโหลดหลายไฟล์
- `GET /files` - ดึงรายการไฟล์
- `GET /files/:id/download` - ดาวน์โหลดไฟล์
- `PUT /files/:id` - อัปเดตข้อมูลไฟล์
- `DELETE /files/:id` - ลบไฟล์ (Soft Delete)
- `PUT /files/:id/restore` - กู้คืนไฟล์
- `DELETE /files/:id/permanent` - ลบไฟล์ถาวร

### Folders
- `POST /folders` - สร้างโฟลเดอร์
- `GET /folders` - ดึงรายการโฟลเดอร์
- `GET /folders/:id` - ดึงข้อมูลโฟลเดอร์
- `PUT /folders/:id` - อัปเดตโฟลเดอร์
- `DELETE /folders/:id` - ลบโฟลเดอร์ (Soft Delete)
- `PUT /folders/:id/restore` - กู้คืนโฟลเดอร์

### Share
- `POST /share/files/:id` - สร้างลิงก์แชร์
- `GET /share/:token` - ดาวน์โหลดผ่านลิงก์แชร์
- `DELETE /share/files/:id` - ยกเลิกลิงก์แชร์
- `GET /share/files/:id/links` - ดึงรายการลิงก์แชร์

## 🎯 การใช้งาน

### 1. อัปโหลดไฟล์
- เข้าหน้า Upload
- เลือกโหมดการอัปโหลด (เดี่ยว/หลายไฟล์)
- ระบุ Owner และโฟลเดอร์ปลายทาง (ถ้ามี)
- เลือกไฟล์และกดอัปโหลด

### 2. จัดการไฟล์
- เข้าหน้า Files
- ดูรายการไฟล์และโฟลเดอร์
- ดาวน์โหลด, แชร์, หรือลบไฟล์
- คลิกโฟลเดอร์เพื่อเข้าไปดูข้างใน

### 3. กู้คืนไฟล์
- เข้าหน้า Trash
- ดูรายการไฟล์ที่ถูกลบ
- กู้คืนหรือลบถาวร

### 4. ตรวจสอบสถานะ
- เข้าหน้า Activities
- กดปุ่มตรวจสอบสถานะ API
- ดูผลการทดสอบและเวลาตอบสนอง

## 🔧 การตั้งค่า

### Environment Variables (Backend)

```env
PORT=3001
NODE_ENV=development
MONGODB_URI=mongodb://root:example@localhost:27017/file-storage?authSource=admin
MAX_FILE_SIZE=107374182400
UPLOAD_PATH=./uploads
APP_URL=http://localhost:3001
```

## 📊 Database Schema

### File
- owner, filename, originalName, mimeType, size
- path, folderId, deletedAt
- shareToken, shareExpiresAt

### Folder
- owner, name, parentId, path
- deletedAt, description

### Share
- fileId, token, expiresAt
- isRevoked, downloadCount, lastAccessedAt

## 🎨 หน้าจอ

- **Home** - หน้าหลักพร้อมเมนูนำทาง
- **Upload** - อัปโหลดไฟล์และโฟลเดอร์
- **Files** - จัดการไฟล์และโฟลเดอร์
- **Trash** - กู้คืนหรือลบไฟล์ถาวร
- **Activities** - ตรวจสอบสถานะระบบ

## 📝 หมายเหตุ

- ระบบใช้ภาษาไทยในทุกส่วน (UI, API Messages, Documentation)
- รองรับ Dark Mode
- ใช้ Soft Delete Pattern (ไฟล์ไม่ถูกลบจริง)
- รองรับการแชร์ไฟล์แบบสาธารณะ
- ไฟล์จะถูกจัดเก็บตาม owner ใน ./uploads/{owner}/

## 🔗 เอกสารเพิ่มเติม

- [DFD Diagram](../exploring-filestorage-project/DFD_DIAGRAM.md)
- [ER Diagram](../exploring-filestorage-project/ER_DIAGRAM.md)
- [Improvements](../exploring-filestorage-project/IMPROVEMENTS.md)

## 📄 License

MIT
